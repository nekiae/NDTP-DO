#!/usr/bin/env python3
"""
Тест сокращения названий документов
"""

from calendar_module import calendar_module

def test_title_shortening():
    """Тестирует функцию сокращения названий документов"""
    print("🧪 ТЕСТИРОВАНИЕ СОКРАЩЕНИЯ НАЗВАНИЙ ДОКУМЕНТОВ")
    print("=" * 60)
    
    # Список тестовых названий документов
    test_titles = [
        "Положение об образовательной смене",
        "Списочный состав участников, допущенных ко второму этапу отбора учащихся для обучения в Национальном детском технопарке с 03.04.2025г. по 26.04.2025г.",
        "Места проведения",
        "Списочный состав групп учащихся, зачисленных в УО Национальный детский технопарк для обучения с 03.04.2025г. по 26.04.2025г.",
        "Очень длинное название документа которое должно быть сокращено до приемлемого размера для отображения в интерфейсе",
        "Короткое название"
    ]
    
    print("\n📋 Результаты сокращения:")
    print("-" * 60)
    
    for i, title in enumerate(test_titles, 1):
        shortened = calendar_module._shorten_document_title(title)
        print(f"\n{i}. ИСХОДНОЕ:")
        print(f"   '{title}'")
        print(f"   📏 Длина: {len(title)} символов")
        print(f"   СОКРАЩЕННОЕ:")
        print(f"   '{shortened}'")
        print(f"   📏 Длина: {len(shortened)} символов")
        print(f"   {'✅ Сокращено' if len(shortened) < len(title) else '⚪ Без изменений'}")

if __name__ == "__main__":
    test_title_shortening() 